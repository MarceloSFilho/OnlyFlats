<div class="my-4">

  <div class="card-trip">
    <%= cl_image_tag flat.photo.key, height: 400, crop: :fill %>
  <div class="card-trip-infos">
    <div>
      <h2><%= flat.title %></h2>
      <p>Address: <%= flat.address %></p>
    </div>
      <h2 class="card-trip-pricing">$ <%= flat.price %></h2>
    </div>
  </div>
</div>
